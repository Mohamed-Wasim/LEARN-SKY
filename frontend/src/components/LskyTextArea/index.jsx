import React from 'react';
import ReactBootstrapTextArea from 'react-bootstrap/FormControl';

const LskyTextArea = (props) => {
  return <ReactBootstrapTextArea as="textarea" {...props} />;
};

export default LskyTextArea;
